[project]
name = "erd"
authors = [{ name = "Ayan Dhara", email = "frappe-erd@ayandhara.me" }]
description = "Frappe Database Visualizer"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
